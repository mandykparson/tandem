import React from 'react'

export default function UserInput() {
    return (
        <div>
            <p>This will be where a user types in a form to render climbers to the screen in "Climber Container"</p>
        </div>
    )
}
